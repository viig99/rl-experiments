# Evolver

## Lunar Lander

To simulate the Lunar Lander environment, we will be using the `envpool` library with an efficient zero implementation.

### Installation

`pip install -r requiruments.txt`